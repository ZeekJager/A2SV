class Solution:
    def maxWidthOfVerticalArea(self, points: List[List[int]]) -> int:
        s = sorted(p[0] for p in points)
        
        max_width = 0
        for i in range(1, len(s)):
            max_width = max(max_width, s[i] - s[i-1])
        
        return max_width