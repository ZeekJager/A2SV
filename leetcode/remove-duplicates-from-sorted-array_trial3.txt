class Solution:
    def removeDuplicates(self, nums: List[int]) -> int:
        n=len(nums)
        l=0
        r = 1
        c = 0

        while r<len(nums):
            if nums[l] == nums[r]:
                del nums[r]
            else:
                l+=1
                r+=1
        k=len(nums)
        print(k)